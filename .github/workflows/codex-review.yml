name: Codex Code Review

on:
  pull_request:
    branches: [development]
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: codex-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  codex-review:
    runs-on: ubuntu-latest
    steps:
      - name: Check if OPENAI_API_KEY is available
        id: check_secret
        run: |
          if [ -z "${{ secrets.OPENAI_API_KEY }}" ]; then
            echo "OPENAI_API_KEY secret is NOT available to this workflow."
            echo "This is expected for PRs from forks. Skipping Codex review."
            echo "secret_available=false" >> $GITHUB_OUTPUT
          else
            echo "OPENAI_API_KEY secret is available."
            echo "secret_available=true" >> $GITHUB_OUTPUT
          fi

      - name: Checkout PR merge commit
        if: steps.check_secret.outputs.secret_available == 'true'
        uses: actions/checkout@v4
        with:
          ref: refs/pull/${{ github.event.pull_request.number }}/merge
          fetch-depth: 0

      - name: Fetch base and head refs
        if: steps.check_secret.outputs.secret_available == 'true'
        run: |
          git fetch --no-tags origin \
            ${{ github.event.pull_request.base.ref }} \
            +refs/pull/${{ github.event.pull_request.number }}/head

      - name: Generate diff for review
        if: steps.check_secret.outputs.secret_available == 'true'
        run: |
          git diff origin/${{ github.event.pull_request.base.ref }}...HEAD > /tmp/pr-diff.txt
          echo "Diff size: $(wc -l < /tmp/pr-diff.txt) lines"

      - name: Setup Node.js
        if: steps.check_secret.outputs.secret_available == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install Codex CLI
        if: steps.check_secret.outputs.secret_available == 'true'
        run: npm install -g @openai/codex

      - name: Run Codex review
        if: steps.check_secret.outputs.secret_available == 'true'
        env:
          CODEX_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          codex exec \
            --full-auto \
            --skip-git-repo-check \
            -o /tmp/review-output.md \
            "You are reviewing a pull request for an Astro + Vue + Tailwind CSS blog site.
          The tech stack is: Astro 5, Vue 3, TypeScript, Tailwind CSS 4.

          The PR diff is in /tmp/pr-diff.txt. Read that file and review the changes for:

          1. **Bugs & Logic Errors** — Incorrect logic, off-by-one errors, null/undefined risks, broken functionality.
          2. **Security** — XSS risks in templates, unsafe data handling, exposed secrets, dependency vulnerabilities.
          3. **Performance** — Unnecessary re-renders, unoptimized images, missing lazy loading, large bundle impacts.
          4. **TypeScript** — Missing types, unsafe any usage, incorrect type assertions.
          5. **Astro & Vue Best Practices** — Component patterns, frontmatter vs client-side code, content collection usage.
          6. **Code Quality** — Dead code, unclear naming, missing error handling, overly complex logic.

          Format your review as a structured markdown report with sections for each category.
          Only include sections where you found issues. If everything looks good in a category, omit it.
          Start with a brief summary of the PR changes and an overall assessment.
          Be constructive and specific — reference file names and line numbers where possible."

      - name: Post review comment on PR
        if: steps.check_secret.outputs.secret_available == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const outputPath = '/tmp/review-output.md';

            if (!fs.existsSync(outputPath)) {
              console.log('No review output file found, skipping comment.');
              return;
            }

            const message = fs.readFileSync(outputPath, 'utf8').trim();

            if (!message) {
              console.log('Review output is empty, skipping comment.');
              return;
            }

            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const header = `## 🤖 Codex Code Review\n\n`;
            const footer = `\n\n---\n*Automated review by [OpenAI Codex CLI](https://github.com/openai/codex) · [run #${context.runNumber}](${runUrl}) · triggered on PR to \`development\`*`;
            const body = header + message + footer;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: body,
            });
            console.log('Posted new review comment');
